# prep

## Parent type

nominal.

## Description

Preposition category subtype.

## Licensed features

| Feature | Expected value |
| --- | --- |
| SELECT | sign-or-none |
| XARG | sign-or-none |
| LID | list(frame) |

## Example JSON

```json
{
  "type": "prep",
  "features": []
}
```

## Notes

The type relies on inherited nominal/category geometry. It does not license `CASE` or verb-only features unless retargeted to a subtype that does.
